import { ReactElement, FC } from "react";
import { Box, Typography } from "@mui/material";


const EditStep: FC = (): ReactElement => {
  return (
    <div>
        Edit Step
    </div>
  );
};

export default EditStep;